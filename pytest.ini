; otel_bridge pytest configuration. norecursedirs is restated in full
; because [pytest] testpaths/norecursedirs REPLACE pytest's defaults,
; not merge with them.
[pytest]
testpaths = tests
norecursedirs = .* *.egg build dist node_modules venv {arch} _darcs CVS __pycache__ docs
